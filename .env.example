VITE_API_URL=https://api.yourdomain.com/api

# Same Google OAuth 2.0 Client ID as server/.env's GOOGLE_CLIENT_ID (this one
# is public — it's meant to be visible in the browser bundle).
VITE_GOOGLE_CLIENT_ID=<from Google Cloud Console>
